PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Supabase → Project Settings → API
# Paste these in server/.env
# Use the service role key only on the server. Never put it in the client.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
